import TypeConfig from "@/type/system/config";

const config: TypeConfig = {
    // 新聞
    news: {
        format: ["jpeg", "png", "gif"],
        path: "/storage/public/news",
        size: 10,
    },
};

export default config;
